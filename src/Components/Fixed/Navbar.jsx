import React from "react";

function Navbar() {
  return (
    <section>
      <div className="container mx-auto ">
        <div>{/* a logo as png */}</div>
        <div>{/* maximum middle with the search bar */}</div>
        <div>{/* Login button, whitelist icon, cart icon */}</div>
      </div>
    </section>
  );
}

export default Navbar;
